name: "01-EKS"
on:
    push:
        branches:
            - feature0.2
jobs:
  Plan:
    uses: anishkasaxena/DevOps_project/.github/workflows/tf_plan.yaml@feature0.2
    
    with:
      gh_environment: dev
    secrets:
      cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

  Apply:
    needs: Plan
    uses: anishkasaxena/DevOps_project/.github/workflows/tf_apply.yaml@feature0.2
    with:
      gh_environment: dev
    secrets:
      cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}



